name: Run Attendance Script

on:
  schedule:
    - cron: '30 13 * * *'

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        pip install -r script/requirements.txt

    - name: Run script
      run: |
        python script/attendance_script.py
